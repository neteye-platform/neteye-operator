---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: keycloakrealms.neteye.cloud
spec:
  group: neteye.cloud
  names:
    kind: KeycloakRealm
    listKind: KeycloakRealmList
    plural: keycloakrealms
    shortNames:
    - kcr
    singular: keycloakrealm
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.realm
      name: Realm
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeycloakRealmSpec declares the desired state of one Keycloak
              realm.
            properties:
              deletionPolicy:
                description: DeletionPolicy decides whether deleting this resource
                  removes the remote realm.
                enum:
                - Orphan
                - Delete
                type: string
              displayName:
                description: DisplayName is the human-readable realm name shown in
                  the Keycloak admin console.
                type: string
              enabled:
                default: true
                description: Enabled enables the realm in Keycloak.
                type: boolean
              realm:
                description: Realm is the Keycloak realm name, which is immutable
                  after creation.
                minLength: 1
                type: string
            required:
            - realm
            type: object
          status:
            description: KeycloakRealmStatus defines the observed state of a KeycloakRealm.
            properties:
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              status:
                description: ServiceState is the per-service state reported in NetEyeServiceStatus.Status.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
